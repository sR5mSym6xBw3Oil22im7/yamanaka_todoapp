# 画面設計書

## 1. 文書の目的

この文書は、やること管理アプリの6画面について、画面に表示する内容、入力方法、エラー文、画面の移動方法を定めるものです。

「画面遷移」とは、ある画面から別の画面へ移動する流れのことです。本アプリで作る画面は、次の6画面だけです。

| 番号 | 画面名 | URL（ページの住所） | 役割 |
|---|---|---|---|
| 1 | 一覧 | `/todos` | 登録済みのやることを見る、検索する、並び替える |
| 2 | 登録 | `/todos/new` | 新しいやることを入力する |
| 3 | 登録の確認 | `/todos/confirm` | 登録前に入力内容を確認する |
| 4 | 編集 | `/todos/{id}/edit` | 登録済みのやることを直す |
| 5 | 編集の確認 | `/todos/{id}/confirm` | 保存前に変更内容を確認する |
| 6 | 削除確認 | `/todos/{id}/delete` | 削除するやることを確認する |

`{id}` は、やることを見分ける番号に置き換えます。ログイン画面は作りません。トップページ `/` は既存のものをそのまま使い、新しい画面としては扱いません。

## 2. 共通の画面設計

### 2.1 共通の見た目

6画面すべてで、共通のCSS（画面の見た目を決める仕組み）`/css/style.css` を読み込みます。画面全体は `container`、画面名は `page-title` を使います。

使うclass（見た目を指定する名前）は、仕様書で指定された次のものだけです。

`container`、`page-title`、`todo-table`、`flash`、`search-bar`、`form-row`、`form-label`、`form-input`、`error`、`confirm-value`、`confirm-actions`、`btn`、`btn sub`、`link`

画面に直接書く見た目の指定、2枚目のCSS、画像、動き、外部CSSは使いません。

### 2.2 共通の表示ルール

- 保存されている優先度 `1`・`2`・`3` は、それぞれ「高」・「中」・「低」と表示します。
- 保存されている完了状態 `TRUE`・`FALSE` は、それぞれ「完了」・「未完了」と表示します。
- 期限が未入力の場合は「（入力なし）」と表示します。
- メモが未入力の場合は「（入力なし）」と表示します。
- `id`、登録日時、更新日時は入力欄や確認内容に表示しません。
- 文字入力では、半角・全角の空白だけの場合は、空欄として扱います。前後に空白がある入力は、その空白を含めて確認します。

## 3. 画面一覧

### 3.1 一覧画面

- URL：`/todos`
- 画面見出し：`やること一覧`
- 使用class：`container`、`page-title`、`flash`、`search-bar`、`todo-table`、`link`、`btn`

#### 画面上部

直前の操作がある場合、次のいずれかを1行だけ表示します。表示場所は一覧の見出しの下です。

| 条件 | 表示する文 |
|---|---|
| 登録が成功した | `登録しました` |
| 編集の保存が成功した | `保存しました` |
| 削除が成功した | `削除しました` |
| 指定した番号が見つからなかった | `見つかりませんでした` |

#### 絞り込みと並び替え

| 項目 | 画面に出す言葉・内容 |
|---|---|
| 名前の入力欄 | ラベル `やることの名前`、入力欄、入力例なし |
| ジャンルの選択欄 | ラベル `ジャンル`、選択肢 `すべて`、`デザイン`、`マーケティング`、`プログラミング`、`資格`、`就職活動` |
| 絞り込みボタン | `絞り込む` |
| 並び替え | `期限が近い順`、`期限が遠い順` |
| 登録ボタン | `新規登録` |

最初に開いたときは、期限が近い順です。絞り込みと並び替えはURLの後ろに条件を付けて行います（例：`/todos?keyword=課題&category=プログラミング&order=asc`）。絞り込み後も、選んだ名前・ジャンル・並び順を画面に保ちます。

名前は一部でも含まれるもの、ジャンルは完全に一致するものを表示します。該当する行が0件の場合も、`まだ1件も登録されていません` と表示します。

#### 一覧表

見出し（表の列名）は左から次の順です。

`やること` ／ `ジャンル` ／ `優先度` ／ `期限` ／ `状態` ／ `操作`

各行の「操作」には、文字リンク `編集` と `削除` を表示します。

期限の並びは、期限が入力されたものを日付順に表示し、期限未入力のものは最後に表示します。期限が近い順では古い日付から、期限が遠い順では新しい日付から表示します。

#### 画面からの移動

- `新規登録` → 登録画面 `/todos/new`
- 行の `編集` → 対象の編集画面 `/todos/{id}/edit`
- 行の `削除` → 対象の削除確認画面 `/todos/{id}/delete`
- `絞り込む` → 条件付きの一覧画面（同じ画面）
- `期限が近い順`、`期限が遠い順` → 選んだ順の一覧画面（同じ画面）

### 3.2 登録画面

- URL：`/todos/new`
- 画面見出し：`やることを登録`
- 使用class：`container`、`page-title`、`form-row`、`form-label`、`form-input`、`error`、`btn`、`btn sub`

| ラベル | 入力方法 | 必須 | エラー文 |
|---|---|---|---|
| `やること` | 1行の文字入力 | 必須 | `やることを入力してください`／`やることは255文字以内で入力してください` |
| `メモ` | 1行の文字入力 | 任意 | `メモは255文字以内で入力してください` |
| `ジャンル` | 選択欄。最初は未選択 | 必須 | `ジャンルを選んでください` |
| `優先度` | 選択欄。最初は未選択 | 必須 | `優先度を選んでください` |
| `期限` | 日付入力 | 任意 | なし |

ジャンルの選択肢は `デザイン`、`マーケティング`、`プログラミング`、`資格`、`就職活動` です。優先度の選択肢は `高`、`中`、`低` です。

ボタンは `確認する` と `一覧へ` です。入力エラーがある場合は保存せず、この画面に留まり、該当する入力欄の下にエラー文を表示します。

#### 画面からの移動

- `確認する` → 登録の確認画面 `/todos/confirm`
- `一覧へ` → 一覧画面 `/todos`

### 3.3 登録の確認画面

- URL：`/todos/confirm`
- 画面見出し：`登録内容の確認`
- 使用class：`container`、`page-title`、`confirm-value`、`confirm-actions`、`btn`、`btn sub`

次の5項目を入力欄ではなく文字で表示します。

`やること`、`メモ`、`ジャンル`、`優先度`、`期限`

未入力の項目は `（入力なし）` と表示します。ボタンは `登録する` と `書き直す` です。

#### 画面からの移動

- `登録する` → 登録処理を行い、一覧 `/todos` へ移動して `登録しました` を表示
- `書き直す` → 入力済みの値を保った登録画面 `/todos/new` へ戻る

### 3.4 編集画面

- URL：`/todos/{id}/edit`
- 画面見出し：`やることを編集`
- 使用class：`container`、`page-title`、`form-row`、`form-label`、`form-input`、`error`、`btn`、`btn sub`

対象の現在の値を入力欄に入れた状態で表示します。入力項目は登録画面の5項目に、次の項目を加えた6項目です。

| ラベル | 入力方法 | 必須 | エラー文 |
|---|---|---|---|
| `やること` | 1行の文字入力 | 必須 | `やることを入力してください`／`やることは255文字以内で入力してください` |
| `メモ` | 1行の文字入力 | 任意 | `メモは255文字以内で入力してください` |
| `ジャンル` | 5種類から選択 | 必須 | `ジャンルを選んでください` |
| `優先度` | `高`・`中`・`低`から選択 | 必須 | `優先度を選んでください` |
| `期限` | 日付入力 | 任意 | なし |
| `完了` | チェック欄（チェックで完了） | 必須項目ではない | なし |

ボタンは `確認する` と `一覧へ` です。エラーがある場合は保存せず、この画面に留まります。

#### 画面からの移動

- `確認する` → 編集の確認画面 `/todos/{id}/confirm`
- `一覧へ` → 一覧画面 `/todos`
- 対象の番号が存在しない → 一覧画面 `/todos` へ戻り、`見つかりませんでした` を表示

### 3.5 編集の確認画面

- URL：`/todos/{id}/confirm`
- 画面見出し：`編集内容の確認`
- 使用class：`container`、`page-title`、`confirm-value`、`confirm-actions`、`btn`、`btn sub`

変更後の次の6項目を、入力欄ではなく文字で表示します。

`やること`、`メモ`、`ジャンル`、`優先度`、`期限`、`完了`

未入力の項目は `（入力なし）` と表示します。完了状態は `完了` または `未完了` と表示します。ボタンは `保存する` と `書き直す` です。

#### 画面からの移動

- `保存する` → 編集処理を行い、一覧 `/todos` へ移動して `保存しました` を表示
- `書き直す` → 入力済みの値を保った編集画面 `/todos/{id}/edit` へ戻る
- 対象の番号が存在しない → 一覧画面 `/todos` へ戻り、`見つかりませんでした` を表示

### 3.6 削除確認画面

- URL：`/todos/{id}/delete`
- 画面見出し：`削除の確認`
- 使用class：`container`、`page-title`、`confirm-value`、`confirm-actions`、`btn`、`btn sub`

削除対象の次の3項目を文字で表示します。

`やること`、`ジャンル`、`期限`

入力欄は表示しません。ボタンは `消す` と `やめる` です。

#### 画面からの移動

- `消す` → 削除処理を行い、一覧 `/todos` へ移動して `削除しました` を表示
- `やめる` → 削除せず一覧画面 `/todos` へ移動
- 対象の番号が存在しない → 一覧画面 `/todos` へ戻り、`見つかりませんでした` を表示

## 4. 画面遷移の全体像

```text
一覧
 ├─「新規登録」→ 登録 ─「確認する」→ 登録の確認 ─「登録する」→ 一覧
 │                         └「書き直す」→ 登録
 ├─行の「編集」→ 編集 ─「確認する」→ 編集の確認 ─「保存する」→ 一覧
 │                       └「書き直す」→ 編集
 └─行の「削除」→ 削除確認 ─「消す」→ 一覧
                              └「やめる」→ 一覧
```

登録・編集・削除が成功した場合は、一覧に戻った直後に結果の文を1行表示します。検索と並び替えは一覧画面のまま内容だけを更新します。

## 5. 保存内容との対応

画面から入力する値は、`todos` テーブル（データを保存する表）の次の項目に対応します。

| 画面の項目 | 保存先 | 備考 |
|---|---|---|
| やること | `title` | 255文字以内、必須 |
| メモ | `detail` | 255文字以内、任意 |
| ジャンル | `category` | 指定された5種類から選択 |
| 優先度 | `priority` | 高=1、中=2、低=3 |
| 期限 | `due_date` | 任意。日付として保存 |
| 完了 | `completed` | チェックあり=完了、なし=未完了 |

`id` は対象を特定する番号、登録日時、更新日時は自動で保存されるため、利用者の入力欄には出しません。

登録・編集・削除が成功したときは、アプリのログ（操作記録）に操作の種類と対象の `id` だけを1行記録します。やることやメモの内容は記録しません。

## 6. 今回の対象外

次の機能・画面は追加しません。

- 7つ目以降の画面
- ログイン、利用者の追加、複数人での共有
- 一覧のページ分け
- 削除した内容を戻す機能
- 完了にした日時の表示や保存
- 写真やファイルの添付
