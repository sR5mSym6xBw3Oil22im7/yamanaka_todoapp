# 要件定義書

## 1. 文書の目的

本書は、やること（Todo）を登録・管理するアプリの要件を定めるものです。
「要件」とは、アプリに必要な機能や守る決まりのことです。

## 2. 対象範囲

### 2.1 作るもの

次の機能を持つ、1人用のやること管理アプリを作ります。

- やることの一覧表示
- やることの登録
- 登録済みのやることの編集
- 登録済みのやることの削除
- やることの名前による絞り込み検索
- 期限による並び替え

ログイン画面は作りません。

### 2.2 利用者

利用者は自分1人です。複数の利用者で共有する機能は対象外です。

## 3. 保存に関する要件

### 3.1 保存先

MySQL（データを保存するソフト）の `todos` テーブルに保存します。1件のやることを1行で保存します。

### 3.2 保存する項目

| 項目 | 内容 | 型（データの種類） | 必須・任意 | 決まり |
|---|---|---|---|---|
| `id` | やることを見分ける番号 | `BIGINT` | 必須 | 自動で1ずつ増える主キー（1件を特定する番号） |
| `title` | やること | `VARCHAR(255)` | 必須 | 255文字以内 |
| `detail` | メモ | `VARCHAR(255)` | 任意 | 255文字以内 |
| `category` | ジャンル | `VARCHAR(255)` | 必須 | 指定された5種類から選ぶ |
| `priority` | 優先度 | `INT` | 必須 | 1＝高、2＝中、3＝低。既定値は2 |
| `due_date` | 期限 | `DATE` | 任意 | — |
| `completed` | 完了したか | `BOOLEAN` | 必須 | 既定値は未完了 |
| `created_at` | 登録日時 | `DATETIME` | 必須 | MySQLが自動で設定 |
| `updated_at` | 最終更新日時 | `DATETIME` | 必須 | MySQLが自動で設定 |

`id`、`created_at`、`updated_at` は利用者の入力欄に表示しません。

### 3.3 ジャンル

`category` は自由入力ではなく、次の5種類から選択します。

1. デザイン
2. マーケティング
3. プログラミング
4. 資格
5. 就職活動

## 4. 機能要件

### 4.1 一覧表示

アドレス `/todos` で、登録済みのやることを一覧表示します。表示項目は次のとおりです。

- やること
- ジャンル
- 優先度
- 期限
- 状態
- 操作（編集・削除）

優先度は保存値の `1`・`2`・`3` を、それぞれ「高」・「中」・「低」と表示します。状態は保存値の `1`・`0` を、それぞれ「完了」・「未完了」と表示します。

登録がない場合、または絞り込み結果が0件の場合は「まだ1件も登録されていません」と表示します。

一覧の上部には、直前の操作結果を1行表示します（5.3参照）。

### 4.2 登録

アドレス `/todos/new` で、新しいやることを入力します。入力項目は次のとおりです。

| 入力項目 | 必須・任意 | 入力方法 |
|---|---|---|
| やること | 必須 | 1行の文字入力 |
| メモ | 任意 | 1行の文字入力 |
| ジャンル | 必須 | 5種類から選択。最初は未選択 |
| 優先度 | 必須 | 高・中・低から選択。最初は未選択 |
| 期限 | 任意 | 日付入力 |

「確認する」で登録確認画面へ進み、「一覧へ」で一覧へ戻ります。

### 4.3 登録確認

アドレス `/todos/confirm` で、入力した5項目を入力欄ではなく文字として表示します。未入力の項目は「（入力なし）」と表示します。

「登録する」で登録して一覧へ戻り、「書き直す」で入力済みの値を保ったまま登録画面へ戻ります。

### 4.4 編集

アドレス `/todos/{id}/edit` で、指定した番号のやることを編集します。`{id}` には対象の番号が入ります。

登録時の5項目に「完了」のチェック欄を加えた6項目を表示し、画面を開いた時点の値を入力欄に入れます。必須項目は、やること・ジャンル・優先度です。

「確認する」で編集確認画面へ進み、「一覧へ」で一覧へ戻ります。

### 4.5 編集確認

アドレス `/todos/{id}/confirm` で、編集内容を文字として表示します。

「保存する」で保存して一覧へ戻り、「書き直す」で入力済みの値を保ったまま編集画面へ戻ります。

### 4.6 削除確認

アドレス `/todos/{id}/delete` で、削除対象のやること・ジャンル・期限を表示します。入力欄は表示しません。

「消す」で削除して一覧へ戻り、「やめる」で削除せず一覧へ戻ります。

### 4.7 トップページ

アドレス `/` にトップページを用意します。トップページは、カード7で作成したものをそのまま使用します。

## 5. 入力・エラーに関する要件

### 5.1 必須入力と文字数

必須項目が空の場合、または文字数を超えた場合は保存しません。入力画面に留まり、該当項目の下に次の文を表示します。

| 条件 | 表示する文 |
|---|---|
| やることが空 | やることを入力してください |
| やることが255文字を超過 | やることは255文字以内で入力してください |
| メモが255文字を超過 | メモは255文字以内で入力してください |
| ジャンルが未選択 | ジャンルを選んでください |
| 優先度が未選択 | 優先度を選んでください |

やること、メモ、ジャンル、優先度の入力チェックを行います。期限には入力チェックの決まりを設けません。

半角スペースまたは全角スペースだけの入力は、空の入力として扱います。

### 5.2 存在しない番号

編集または削除確認で、`todos` テーブルに存在しない番号が指定された場合は、専用画面を表示しません。一覧へ戻し、上部に「見つかりませんでした」と表示します。

### 5.3 操作結果の表示

登録・編集・削除などの操作後は、一覧の上部に次の文を1行表示します。

| 操作 | 表示する文 |
|---|---|
| 登録後 | 登録しました |
| 編集の保存後 | 保存しました |
| 削除後 | 削除しました |
| 存在しない番号を開いた場合 | 見つかりませんでした |

## 6. 検索・並び替え要件

### 6.1 絞り込み

一覧には、やることの名前による絞り込み入力欄と、ジャンルによる絞り込み選択欄を設けます。ジャンルで「すべて」を選ぶと絞り込みません。

- 名前は、`title` に入力した言葉が一部でも含まれるものを残します。
- ジャンルは、`category` が選択値と完全に一致するものだけを残します。
- 名前とジャンルの両方が空または「すべて」の場合は、全件を表示します。
- 絞り込み条件と並び順は、アドレスの後ろに付けて送ります。例：`/todos?keyword=課題&category=プログラミング&order=asc`
- 条件に一致するものがない場合は、「まだ1件も登録されていません」と表示します。

### 6.2 並び替え

一覧に「期限が近い順」と「期限が遠い順」のリンクを設けます。最初に一覧を開いたときは、期限が近い順に表示します。

## 7. 画面と画面遷移

「画面遷移」とは、ある画面から別の画面へ移動する流れのことです。

| 画面 | アドレス | 役割 |
|---|---|---|
| 一覧 | `/todos` | やることを一覧表示 |
| 登録 | `/todos/new` | 新しいやることを入力 |
| 登録確認 | `/todos/confirm` | 登録前の内容を確認 |
| 編集 | `/todos/{id}/edit` | 既存のやることを編集 |
| 編集確認 | `/todos/{id}/confirm` | 保存前の内容を確認 |
| 削除確認 | `/todos/{id}/delete` | 削除前の内容を確認 |
| トップページ | `/` | カード7で作成したものを使用 |

一覧から新規登録・編集・削除へ移動できます。登録と編集は確認画面を経由して一覧へ戻ります。削除確認は、削除または取りやめのどちらでも一覧へ戻ります。

## 8. 記録（ログ）要件

「ログ」とは、操作の記録のことです。登録・編集・削除が実行されたとき、1操作につき1行の記録を残します。

- 記録する内容：操作の種類（登録・編集・削除）と対象の `id`
- 記録しない内容：`title` と `detail` の中身

## 9. 見た目に関する要件

CSS（画面の見た目を指定する仕組み）は1ファイルだけ作成し、6画面すべてで読み込みます。

- ファイル：`src/main/resources/static/css/style.css`
- 各テンプレートの `<head>` に、`<link rel="stylesheet" href="/css/style.css">` を1行入れます。
- 画面内に直接書く `style="..."` は使いません。
- CSSファイルは2枚目を作りません。
- 使用するclass名は叩き台で指定されたものだけとし、新しいclass名は追加しません。
- 影、動き、画像、外部から読み込むCSSは使いません。

使用するclass名は次のとおりです。

`container`、`page-title`、`todo-table`、`flash`、`search-bar`、`form-row`、`form-label`、`form-input`、`error`、`confirm-value`、`confirm-actions`、`btn`、`btn sub`、`link`

CSSの具体的な内容は、叩き台の6.3に記載された内容に従います。

## 10. 今回は作らないもの

- 一覧のページ分け
- 削除したものを後から戻す機能
- 完了にした日時の記録
- ログイン
- 複数人での共有
- 写真やファイルの添付

上記以外についても、叩き台に書かれていない機能は今回は作りません。

## 11. 叩き台からの補足

本書の「文書の目的」「対象範囲」「機能要件」などの区分と、「要件」「型」「画面遷移」「ログ」「CSS」などの短い言い換えは、叩き台の内容を要件定義書として読みやすく整理するために追加しました。

新しい機能、画面、保存項目、入力条件、見た目の決まりは追加していません。
